<form method="post" action="{{ path('game_delete', {'id': gameDetails.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gameDetails.id) }}">
	<button class="btn">Delete</button>
</form>
